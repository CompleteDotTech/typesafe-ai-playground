[project]
name = "dspy-jev-optimizer-starter"
version = "0.1.0"
description = "DSPy outer-loop optimization of TypeSafe Jev prompts"
requires-python = ">=3.10"
dependencies = [
  "dspy>=3.0",
  "typesafe-sdk",
]

[tool.ruff]
line-length = 110
